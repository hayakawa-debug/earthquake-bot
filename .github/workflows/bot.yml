name: EarthquakeBot

on:
  schedule:
    - cron: "*/5 * * * *"  # 5分ごとに実行
  workflow_dispatch:       # 手動実行も可能

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: 3.11
      - run: pip install tweepy feedparser
      - run: python bot.py
        env:
          API_KEY: ${{ BhyXOHjAKtDprBFDbSJFVxqxd }}
          API_SECRET: ${{ BUIEYkVKjtemb7DIItqdqPvGuvkR6ziGqEZqhfAEebAj30uJpf }}
          ACCESS_TOKEN: ${{ 1658403711071457280-eJ5BI6EDDefwEQLNLb9woeyINqvb37
 }}
          ACCESS_SECRET: ${{ 8rgJJu0wBLaBp1EEXstmiT5Stu7zL61JMAiNZXttiuTT6 }}
